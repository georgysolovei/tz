version: '3.8'

services:
  backend:
    build: ./backend
    expose:
      - "8080"

  nginx:
    image: nginx:1.30.2-alpine
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on: 
      - backend
  
